"""
# 请使用代码案例说明 Python 中的标识符命名规则，以'''开始输出。
# 标识符可以是小写字母（a 到 z）或大写字母（A 到 Z）或数字（0 到 9）或下划线（_）的组合。
# 标识符不能以数字开头。
# 标识符是区分大小写的。
# 示例：

variable = 10
Variable = 20
print(variable)  # 输出：10
print(Variable)  # 输出：20

"""

# 如何使用Python中的三元表达式？举例展示：
"""
三元表达式是一种条件表达式，可以用一行代码实现 if-else 语句。
语法：
x if condition else y
示例：

a = 10
b = 20
max = a if a > b else b
print(max)  # 输出：20
"""

a = 10
b = 20
max = a if a > b else b
print(max)  # 输出：20

# 如何在Python中使用二元运算符和比较运算符？举例展示：
"""
二元运算符是操作数为两个的运算符，例如加法运算符（+）、减法运算符（-）、乘法运算符（*）、除法运算符（/）等。
比较运算符用于比较两个值，例如等于（==）、不等于（!=）、大于（>）、小于（<）、大于等于（>=）、小于等于（<=）等。
示例：

a = 10
b = 20
print(a + b)  # 输出：30
print(a - b)  # 输出：-10
print(a * b)  # 输出：200
print(a / b)  # 输出：0.5
print(a == b)  # 输出：False
print(a != b)  # 输出：True
print(a > b)  # 输出：False
print(a < b)  # 输出：True
print(a >= b)  # 输出：False
print(a <= b)  # 输出：True
"""
a = 10
b = 20
print(a + b)  # 输出：30
print(a - b)  # 输出：-10
print(a * b)  # 输出：200
print(a / b)  # 输出：0.5
print(a == b)  # 输出：False
print(a != b)  # 输出：True
print(a > b)  # 输出：False
print(a < b)  # 输出：True
print(a >= b)  # 输出：False
print(a <= b)  # 输出：True