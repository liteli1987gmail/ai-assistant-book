"""
# 请使用代码案例说明 Python 中的标识符命名规则，以'''开始输出。
# 标识符可以是小写字母（a 到 z）或大写字母（A 到 Z）或数字（0 到 9）或下划线（_）的组合。
# 标识符不能以数字开头。
# 标识符是区分大小写的。
# 示例：

variable = 10
Variable = 20
print(variable)  # 输出：10
print(Variable)  # 输出：20

"""

# 什么是Python中的二元运算符和比较运算符？举例展示：
"""
    Python 中的二元运算符是需要两个操作数来进行运算的运算符，如加法运算符（+）、减法运算符（-）等。
    Python 中的比较运算符是用来比较两个值的运算符，如等于运算符（==）、大于运算符（>）等。
    示例：
    a = 10
    b = 20
    c = a + b
    print(c)  # 输出：30
    print(a == b)  # 输出：False
"""
a = 10
b = 20
c = a + b
print(c)  # 输出：30
print(a == b)  # 输出：False


# 如何判断两个对象是否相同？举例展示：
"""
    Python 中可以使用“==”运算符来判断两个对象的值是否相同，可以使用“is”运算符来判断两个对象的引用是否相同。
    示例：
    a = 10
    b = 10
    print(a == b)  # 输出：True
    print(a is b)  # 输出：True
"""

a = 10
b = 10
print(a == b)  # 输出：True
print(a is b)  # 输出：True