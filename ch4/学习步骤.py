"""
# 请使用代码案例说明 Python 中的标识符命名规则，以"""开始输出。
# 标识符可以是小写字母（a 到 z）或大写字母（A 到 Z）或数字（0 到 9）或下划线（_）的组合。
# 标识符不能以数字开头。
# 标识符是区分大小写的。
# 示例：

variable = 10
Variable = 20
print(variable)  # 输出：10
print(Variable)  # 输出：20

"""


# 请使用代码案例说明 Python　的if-else语句，以"""开始输出。
"""
    Python 中的 if-else 语句用于控制程序的流程，根据条件判断执行不同的代码块。
    语法：
    if 条件:
        代码块
    else:
        代码块
    示例：
    x = 0
    if x < 10:
        print("x is less than 10")
        print("Inside the if block")
    else:
        print("x is greater than or equal to 10")
"""

x = 0
if x < 10:
    print("x is less than 10")
    print("Inside the if block")
else:
    print("x is greater than or equal to 10")


# 请使用代码案例说明 Python 中的注释方法。

# 请使用代码案例说明 Python 中的变量赋值和类型。
"""
    变量赋值
    变量赋值是通过赋值运算符（=）来实现的。在 Python 中，变量是没有类型的，变量可以引用任何类型的对象。
    语法：
    变量名 = 值
    示例：
    a = 10
    b = 20
    c = a + b
    print(c)  # 输出：30
"""

a = 10
b = 20
c = a + b
print(c)  # 输出：30














